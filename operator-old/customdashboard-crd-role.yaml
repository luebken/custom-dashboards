apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: customdashboard-crd
rules:
- apiGroups:
  - instana.io
  resources:
    - customdashboards
    - customdashboards/finalizers
  verbs: [get, list, create, update, delete, deletecollection, watch]